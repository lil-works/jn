{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <h1>Instrumentfamily creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('instrumentfamily_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
